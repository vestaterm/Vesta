-- Optional. If present, Vesta reads it for the plugin's version and load priority.
-- Higher priority loads earlier (ties broken by name); default priority is 0.
return {
  version = "1.0.0",
  priority = 0,
}
