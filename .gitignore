.build/
*.xcframework/
.DS_Store
.superpowers/

# built app bundle (use ./make-app.sh)
Vesta.app/
*.app/

# local-only build/dev artifacts (root-anchored so Sources/Vesta/Resources stays tracked)
/Resources/
/mockup.html
# make-app.sh is tracked again — the release/CI workflows build the app with it.
.claude/
